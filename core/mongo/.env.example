# Used by container kage on first boot of an empty volume only.
# Changing these later does not create the user. See README (UserNotFound).
# Must match the user and password inside services/discord-bot MONGO_URI.
MONGO_INITDB_ROOT_USERNAME=""
MONGO_INITDB_ROOT_PASSWORD=""